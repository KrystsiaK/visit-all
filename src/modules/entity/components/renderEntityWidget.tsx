// REPLACED: renderEntityWidget is no longer used.
// Widget routing now happens via ENTITY_WIDGET_REGISTRY in src/lib/widget-manifest.ts.
// Each widget reads its data from EntityContext (entity-context.tsx) instead of receiving props.
// Kept here for reference only — do not import.
export {};
